// Functions
const fullName = function(firstName, lastName) {
  return firstName + " " + lastName;
};
let firstName = 'joe';
let lastName = 'momma';
const hisName = fullName(firstName, lastName);



// Lambda
const fullNameLambda = (firstName, lastName) => {
  return firstName + " " + lastName;
};

